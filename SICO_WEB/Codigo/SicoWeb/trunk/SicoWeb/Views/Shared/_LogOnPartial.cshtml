@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong> @Html.ActionLink("Cerrar Sessión", "LogOff", "Account")</text>
}
else {
     @Html.ActionLink("Inicio Sessión", "LogOn", "Account") 
}
